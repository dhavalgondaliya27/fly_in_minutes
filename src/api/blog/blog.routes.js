const express = require('express');
const router = express.Router();
const controller = require('./blog.controller');
const validator = require('./blog.validator');
// Routes will be added here

module.exports = router; 